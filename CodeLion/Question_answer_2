total_list = []

#질문 입력
while True:
  question = input("질문을 입력해 주세요 : ")
  if (question == "q"):
    break
  else:
    total_list.append({"질문":[question], "답변":" "})

#답변 입력
for i in total_list:
  print(i["질문"])
  answer = input("답변을 입력해 주세요 : ")
  i["답변"] answer

#최종 결과물 
print(total_list)
